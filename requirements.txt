# Версии зафиксированы по фактически установленным на машине прогона.
# torch стоит из индекса cu128 и здесь НЕ переустанавливается: строка
# приведена для протокола, ставить его нужно командой из README.
#
#   pip install -r requirements.txt
#
# Правило 6 проекта: имя весов, sha256, imgsz, device и precision пишутся
# в run_manifest.json на каждом прогоне. Пин версий здесь — только половина
# воспроизводимости, вторая половина в манифесте.

# --- вывод моделей -------------------------------------------------------- #
ultralytics==8.4.138          # AGPL-3.0. См. раздел License в README.
# torch и torchvision ставятся отдельно, с индексом CUDA:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
lapx==0.9.4                   # венгерский алгоритм для ByteTrack
# filterpy НЕ нужен: ByteTracker из ultralytics везёт собственный фильтр Калмана.

# --- данные и геометрия --------------------------------------------------- #
numpy==1.26.4
opencv-python==4.9.0.80
pandas==2.1.4
pyarrow==25.0.1               # parquet: формат всех артефактов между этапами
scipy==1.15.3
shapely==2.1.2
PyYAML==6.0.2                 # конфиги этапов

# --- графики выбора часа -------------------------------------------------- #
matplotlib==3.8.4

# --- тесты ---------------------------------------------------------------- #
pytest==7.4.3
